import test from 'node:test';import assert from 'node:assert/strict';import { createApp } from '../src/app.js'
test('health endpoint responds',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/health`);assert.equal(response.status,200);assert.deepEqual(await response.json(),{ok:true});server.close()})
test('federation discovery advertises protocol',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/.well-known/libracord`);assert.equal((await response.json()).protocol,'libracord');server.close()})
test('federation discovery respects reverse proxy origin',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/.well-known/libracord`,{headers:{'x-forwarded-proto':'https','x-forwarded-host':'chat.example.test'}}),body=await response.json();assert.equal(body.instance,'https://chat.example.test');assert.equal(body.api,'https://chat.example.test/api/v1');server.close()})
test('private APIs require authentication',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/api/communities`);assert.equal(response.status,401);server.close()})
test('v1 exposes public instance metadata',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/api/v1/instance`),body=await response.json();assert.equal(response.status,200);assert.equal(body.api_version,1);assert.equal(typeof body.name,'string');server.close()})
test('unknown UUID asset returns 404',async()=>{const server=createApp().listen(0),{port}=server.address();const response=await fetch(`http://127.0.0.1:${port}/api/v1/assets/00000000-0000-4000-8000-000000000000`);assert.equal(response.status,404);server.close()})
